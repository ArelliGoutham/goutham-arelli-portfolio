name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Lint · Type check · Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # Skip lint and type-check on Dependabot PRs — only verify the build compiles
      - name: Lint
        if: github.actor != 'dependabot[bot]'
        run: npm run lint

      - name: Type check
        if: github.actor != 'dependabot[bot]'
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
